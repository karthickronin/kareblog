import AddPost from '../../components/AddPost';

export const metadata = {
    title: "Add Post Page",
    description: "Karthick's add post page"
}

export default function Page() {
   return <AddPost />
}